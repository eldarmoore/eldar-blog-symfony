{% extends 'base.html.twig' %}

{% block title %} Welcome to blog Page{% endblock %}

{% block body %}
    <div class="col-md-12 blog-main">
        <h1>{{ blog.title }}</h1>
        <p class="article-justify">{{ blog.article }}</p>
        <p>{{ blog.blogDate|date('H:m d/m/Y') }}</p>
        <a href="{{ path('index_page') }}">Return Home</a>
    </div>
{% endblock %}
